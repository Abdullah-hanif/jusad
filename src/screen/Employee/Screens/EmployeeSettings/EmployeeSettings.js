import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const EmployeeSettings = () => {
  return (
    <View>
      <Text>EmployeeSettings</Text>
    </View>
  );
};

export default EmployeeSettings;

const styles = StyleSheet.create({});
